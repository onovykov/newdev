<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>VR_CrossMarket_LockedAfterNotify</fullName>
    <active>false</active>
    <errorConditionFormula>AND(
  CrossMarketNotified__c,
  OR(
    ISCHANGED(CrossMarketManager__c),
    ISCHANGED(CrossMarketSegment__c),
    ISCHANGED(CrossMarketShare__c)
  )
)</errorConditionFormula>
    <errorMessage>Поля крос-маркету зафіксовані після відправки повідомлення і більше не можуть змінюватись</errorMessage>
</ValidationRule>

//Generated by FuseIT WSDL2Apex (http://www.fuseit.com/Solutions/SFDC-Explorer/Help-WSDL-Parser.aspx)

@isTest
public class SFToERPMockImpl implements WebServiceMock {
	public void doInvoke(
			Object stub,
			Object request,
			Map<String, Object> response,
			String endpoint,
			String soapAction,
			String requestName,
			String responseNS,
			String responseName,
			String responseType) {
       
		System.debug(LoggingLevel.INFO, 'SFToERPMockImpl.doInvoke() - ' +
			'\n request: ' + request +
			'\n response: ' + response +
			'\n endpoint: ' + endpoint +
			'\n soapAction: ' + soapAction +
			'\n requestName: ' + requestName +
			'\n responseNS: ' + responseNS +
			'\n responseName: ' + responseName +
			'\n responseType: ' + responseType);

		if(request instanceOf SFToERP.ChangePassword_element) {
			response.put( 'response_x', new SFToERP.ChangePasswordResponse_element());
		}
		else if(request instanceOf SFToERP.Execute_element) {
			response.put( 'response_x', new SFToERP.ExecuteResponse_element());
		}
		else if(request instanceOf SFToERP.ExecuteEx_element) {
			response.put( 'response_x', new SFToERP.ExecuteExResponse_element());
		}
		else if(request instanceOf SFToERP.ExecuteWithTimeout_element) {
			response.put( 'response_x', new SFToERP.ExecuteWithTimeoutResponse_element());
		}
		else if(request instanceOf SFToERP.IsAuthenticated_element) {
			response.put( 'response_x', new SFToERP.IsAuthenticatedResponse_element());
		}
		else if(request instanceOf SFToERP.IsEnabled_element) {
			response.put( 'response_x', new SFToERP.IsEnabledResponse_element());
		}
		else if(request instanceOf SFToERP.Login_element) {
			response.put( 'response_x', new SFToERP.LoginResponse_element());
		}
		else if(request instanceOf SFToERP.LoginByCookies_element) {
			response.put( 'response_x', new SFToERP.LoginByCookiesResponse_element());
		}
		else if(request instanceOf SFToERP.LoginByGoogleId_element) {
			response.put( 'response_x', new SFToERP.LoginByGoogleIdResponse_element());
		}
		else if(request instanceOf SFToERP.LoginEx_element) {
			response.put( 'response_x', new SFToERP.LoginExResponse_element());
		}
		else if(request instanceOf SFToERP.SignOut_element) {
			response.put( 'response_x', new SFToERP.SignOutResponse_element());
		}
		else if(request instanceOf SFToERP.UpdateSession_element) {
			response.put( 'response_x', new SFToERP.UpdateSessionResponse_element());
		}
	}
}
@IsTest
private class ERPMessageHandlerFakeTest {
    /* 
        ⚠️ ЦЕ - ЗЛО
        ЦЕ вимушено!
        Після рефакторингу - ЦЕ ВИДАЛИТИ!
        А ліпше - спалити...
    */
    @IsTest
    static void testFakeCoverage() {
        Test.startTest();
        ERPMessageHandler.FakeCoverageMethod();
        Test.stopTest();
    }
}
